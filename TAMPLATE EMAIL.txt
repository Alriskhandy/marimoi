
### ✅ 2. Jalankan Permanen dengan Supervisor (disarankan)

1. **Install Supervisor**

   ```bash
   sudo apt update
   sudo apt install supervisor -y
   ```

2. **Buat Config Baru**
   Misalnya:

   ```bash
   sudo nano /etc/supervisor/conf.d/laravel-worker.conf
   ```

   Isinya:

   ```
   [program:laravel-worker]
   process_name=%(program_name)s_%(process_num)02d
   command=php /var/www/html/artisan queue:work --tries=3 --sleep=3 --timeout=90
   autostart=true
   autorestart=true
   user=www-data
   numprocs=1
   redirect_stderr=true
   stdout_logfile=/var/www/html/storage/logs/worker.log
   stopwaitsecs=3600
   ```

   ⚠️ Ganti `/var/www/html` sesuai path project Laravel Anda.

3. **Reload Supervisor & Jalankan**

   ```bash
   sudo supervisorctl reread
   sudo supervisorctl update
   sudo supervisorctl start laravel-worker:*
   ```

4. **Cek Status**

   ```bash
   sudo supervisorctl status laravel-worker:*
   ```

5. **Cek Log Queue Worker**

   ```bash
   tail -f /var/www/html/storage/logs/worker.log
   ```

---

### ✅ 3. Tips Tambahan

* Gunakan `--sleep=3` agar worker istirahat sebentar kalau tidak ada job.
* Gunakan `--timeout=90` agar job panjang tidak otomatis dihentikan terlalu cepat.
* Di production, selalu pakai `queue:work` (bukan `queue:listen`, karena lebih efisien).

